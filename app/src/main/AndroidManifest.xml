<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.seedsofliteracy.play2readv2" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".OpeningActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".showSortWordsGameController"
            android:label="@string/title_activity_show_sort_words_game_controller" >
        </activity>
        <activity
            android:name=".showLesson"
            android:label="@string/title_activity_show_lesson" >
        </activity>
        <activity
            android:name=".MyPdfViewerActivity"
            android:label="Lesson Viewer" >
        </activity>
    </application>

</manifest>
